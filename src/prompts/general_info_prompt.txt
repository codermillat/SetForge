You are a data summarization assistant. Your task is to process the following text and format it as a JSON object that adheres to the provided schema.

**Instructions:**

1.  Read the text content.
2.  Write a brief, one-paragraph summary of the text.
3.  Create a JSON object with two keys:
    *   `"summary"`: Your generated summary.
    *   `"raw_content"`: The original, verbatim text content.
4.  Ensure your output is a single, well-formed JSON object.

**JSON Schema:**

```json
{{
  "title": "General Information Schema",
  "description": "A fallback schema for general, unstructured information from a document.",
  "type": "object",
  "properties": {{
    "summary": {{
      "type": "string",
      "description": "A brief summary of the provided text content."
    }},
    "raw_content": {{
      "type": "string",
      "description": "The original, verbatim text content."
    }}
  }},
  "required": ["summary", "raw_content"]
}}
```

**Text to Analyze:**

```
{text_content}
```

**Your JSON Output:**
