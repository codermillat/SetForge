You are a data extraction specialist. Your task is to extract admission requirement information from the following text and format it as a JSON object that adheres to the provided schema.

**Instructions:**

1.  Carefully read the text to identify all mentions of admission requirements.
2.  Extract the eligibility criteria for both national and international students if available.
3.  Extract any description of the application process.
4.  Extract a list of any required admission tests (e.g., SUAT, JEE).
5.  Construct a JSON object that strictly follows the structure and data types defined in the JSON schema below.
6.  If certain information is not present in the text, omit the corresponding key from the JSON object. Do not invent data.

**JSON Schema:**

```json
{{
  "title": "Admission Requirements Schema",
  "description": "Schema for capturing admission requirements for a program.",
  "type": "object",
  "properties": {{
    "eligibility_criteria": {{
      "type": "object",
      "description": "Eligibility criteria for admission.",
      "properties": {{
        "national_students": {{
          "type": "string",
          "description": "Eligibility criteria for national students."
        }},
        "international_students": {{
          "type": "string",
          "description": "Eligibility criteria for international students."
        }}
      }}
    }},
    "application_process": {{
      "type": "string",
      "description": "Description of the application process."
    }},
    "required_tests": {{
      "type": "array",
      "description": "A list of required admission tests.",
      "items": {{
        "type": "string",
        "description": "The name of the required test (e.g., 'SUAT', 'JEE')."
      }}
    }}
  }},
  "required": ["eligibility_criteria"]
}}
```

**Text to Analyze:**

```
{text_content}
```

**Your JSON Output:**
