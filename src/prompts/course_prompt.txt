You are a data extraction specialist. Your task is to extract course curriculum information from the following text and format it as a JSON object that adheres to the provided schema.

**Instructions:**

1.  Carefully read the text to identify the main course being described.
2.  Extract the course name, course code, and duration.
3.  For each semester, extract the semester number and a list of all subjects.
4.  For each subject, extract its name, code, and credits if available.
5.  Construct a JSON object that strictly follows the structure and data types defined in the JSON schema below.
6.  If certain information is not present in the text, omit the corresponding key from the JSON object. Do not invent data.

**JSON Schema:**

```json
{{
  "title": "Course Curriculum Schema",
  "description": "Schema for capturing detailed information about the curriculum of a course.",
  "type": "object",
  "properties": {{
    "course_name": {{
      "type": "string",
      "description": "The name of the course (e.g., 'Bachelor of Technology (Computer Science & Engineering)')."
    }},
    "course_code": {{
      "type": "string",
      "description": "The code for the course (e.g., 'SET0101')."
    }},
    "duration_years": {{
      "type": "number",
      "description": "The duration of the course in years."
    }},
    "semesters": {{
      "type": "array",
      "description": "A list of semesters, each containing a list of subjects.",
      "items": {{
        "type": "object",
        "properties": {{
          "semester_number": {{
            "type": "integer",
            "description": "The semester number (e.g., 1, 2)."
          }},
          "subjects": {{
            "type": "array",
            "description": "A list of subjects taught in this semester.",
            "items": {{
              "type": "object",
              "properties": {{
                "subject_name": {{
                  "type": "string"
                }},
                "subject_code": {{
                  "type": "string"
                }},
                "credits": {{
                  "type": "number"
                }}
              }},
              "required": ["subject_name", "subject_code"]
            }}
          }}
        }},
        "required": ["semester_number", "subjects"]
      }}
    }}
  }},
  "required": ["course_name", "semesters"]
}}
```

**Text to Analyze:**

```
{text_content}
```

**Your JSON Output:**
