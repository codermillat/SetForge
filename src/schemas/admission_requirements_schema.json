{
  "type": "object",
  "properties": {
    "admission_requirements": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "program_name": {
            "type": "string",
            "description": "The name of the academic program."
          },
          "eligibility_criteria": {
            "type": "string",
            "description": "The eligibility criteria for the program."
          },
          "required_documents": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "A list of required documents for admission."
          },
          "application_deadline": {
            "type": "string",
            "description": "The application deadline for the program."
          }
        },
        "required": ["program_name", "eligibility_criteria", "required_documents", "application_deadline"]
      }
    }
  },
  "required": ["admission_requirements"]
}
