{
  "type": "object",
  "properties": {
    "student_feedback": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "student_name": {
            "type": "string",
            "description": "The name of the student providing the feedback."
          },
          "course_name": {
            "type": "string",
            "description": "The course the student is enrolled in."
          },
          "feedback_text": {
            "type": "string",
            "description": "The text of the student's feedback or testimonial."
          },
          "feedback_date": {
            "type": "string",
            "description": "The date the feedback was provided."
          }
        },
        "required": ["student_name", "course_name", "feedback_text", "feedback_date"]
      }
    }
  },
  "required": ["student_feedback"]
}
